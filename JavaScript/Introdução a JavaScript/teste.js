//var y = 0; // serve para comentar uma linha

/* var y = 0;
var x = 1; */ /* serve para comentar varias linhas */

// declarar variavel no JS, pode ser usado const (valores que não pode ser manipulados) ou pode ser usado var (valores que podem ser manipulados)

// sintaxe de uma função
function soma(a, b){
    return a + b; // 8
}

// chamar a função
soma(3, 5);